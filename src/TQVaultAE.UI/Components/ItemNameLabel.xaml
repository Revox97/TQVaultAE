<UserControl x:Class="TQVaultAE.UI.Components.ItemNameLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TQVaultAE.UI.Components"
             mc:Ignorable="d" 
             HorizontalAlignment="Left"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/TQVaultAE.UI;component/Templates/ItemNameLabelTemplate.xaml"/>
                <ResourceDictionary>
                    <Style x:Key="ItemNameLabel" TargetType="{x:Type Label}">
                        <Setter Property="Template" Value="{StaticResource ItemNameLabelTemplate}"/>
                        <Setter Property="BorderBrush" Value="Gold"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Background" Value="DimGray"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="2"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="ItemNameContainer" HorizontalAlignment="Stretch">
        <Label x:Name="ItemNameContent" HorizontalAlignment="Stretch" Style="{StaticResource ItemNameLabel}" Grid.Row="1" Margin="30 0 0 0"/>
    </Grid>
</UserControl>
